package com.slick2d.snake.game.object.bonus;

/**
 * Created by LilG2pac on 29.05.2018.
 */
public class ObjectDisabler {


    /*private int initialDelay;

    private int period;

    private ScheduledExecutorService executor;

    public BonusFactory(int initialDelay0, int period1) {
        this.initialDelay = initialDelay;
        this.period = period;
        this.executor = Executors.newScheduledThreadPool(1);

    }

    public void generateBonus(Bonus bonus) {
        // Runnable task = () -> generateBonus2(bonus);
        // executor.scheduleAtFixedRate(task, initialDelay, period, TimeUnit.SECONDS);
    */
}
